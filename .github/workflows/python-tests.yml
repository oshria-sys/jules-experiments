name: CI and Deploy Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run pytest
        run: |
          if ! command -v pytest >/dev/null 2>&1; then pip install pytest; fi
          pytest

  deploy:
    name: deploy
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Ensure index.html at repo root (copy from docs if present)
        run: |
          if [ -f docs/index.html ]; then cp docs/index.html index.html; fi

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./index.html

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1
```# filepath: /Users/oshria/gitHub/jules-experiments/.github/workflows/python-tests.yml
name: CI and Deploy Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run pytest
        run: |
          if ! command -v pytest >/dev/null 2>&1; then pip install pytest; fi
          pytest

  deploy:
    name: deploy
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Ensure index.html at repo root (copy from docs if present)
        run: |
          if [ -f docs/index.html ]; then cp docs/index.html index.html; fi

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./index.html

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1